// swissknife.go — deleted. The cvmfs_swissknife ingest path has been removed.
// The bits pipeline (compress → CAS → BuildSubtree → gateway commit) is the
// only supported publish path. See subtree.go for the catalog build step.
package cvmfscatalog
